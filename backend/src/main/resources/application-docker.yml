spring:
  datasource:
    url: jdbc:mysql://host.docker.internal:3306/ai_notes?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=GMT%2B8&allowPublicKeyRetrieval=true
    username: root
    password: 123456
  data:
    redis:
      host: redis
      port: 6379
      password:

minio:
  endpoint: http://minio:9000
  access-key: minioadmin
  secret-key: minioadmin
